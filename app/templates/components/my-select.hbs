<select {{action 'change' on='change'}} class="form-control">
    {{#each content key="@index" as |item|}}
        <option value="{{item}}" selected={{is-equal item.value selectedValue}}>
            {{item.text}}
        </option>
    {{/each}}
</select>